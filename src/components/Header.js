import React from 'react'
import { Typography } from '@material-ui/core'

const Header = () => {
    return (
        <div>
            <Typography variant='h1' align='center'>React mini Project</Typography>
        </div>
    )
}

export default Header
